# Notes

- This export prioritizes transparency over refactors.
- Anything unclear/unmapped was placed in `legacy/` rather than deleted.
- For local development with Codex, consider adding:
  - typed config layer in `app/config.py`
  - unit tests in `tests/`
